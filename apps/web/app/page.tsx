export default function HomePage() {
  return (
    <main className="grid min-h-screen place-items-center p-8">
      <section className="max-w-xl rounded-xl bg-white p-8 shadow-sm">
        <p className="text-sm font-medium text-slate-500">ERP Next</p>
        <h1 className="mt-2 text-3xl font-semibold">Estrutura inicial pronta</h1>
        <p className="mt-3 text-slate-600">
          A interface e os módulos do ERP serão construídos nas próximas etapas.
        </p>
      </section>
    </main>
  );
}
